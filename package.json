{
  "name": "ccron",
  "version": "0.1.0",
  "description": "Schedule claude -p execution on macOS with launchd",
  "type": "module",
  "bin": {
    "ccron": "src/cli.ts"
  },
  "files": [
    "src",
    "claude-plugin"
  ],
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
